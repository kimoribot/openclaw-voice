# OpenClaw Discord Voice Configuration

# Required
DISCORD_BOT_TOKEN=your_discord_bot_token

# Bot identity
BOT_NAME=OpenClaw
DEFAULT_VOLUME=0.8

# Port for API server (OpenClaw calls this to trigger voice)
NOTIFIER_PORT=5000

# Verbosity: silent | minimal | normal | verbose
# - silent: Only errors
# - minimal: Only important info (now playing, errors)  
# - normal: Standard responses
# - verbose: All debug info
VERBOSITY=minimal

# Text channel response when speaking in voice
# Options: always | never | errors_only
TEXT_RESPONSE=always